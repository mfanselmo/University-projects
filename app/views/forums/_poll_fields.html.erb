<fieldset>
  <div class="field">
    <%= ff.label :title %>
    <%= ff.text_field :title, id: :poll_title %>
  </div>
  <%= ff.label :post_id %>
  <%= ff.number_field :post_id, :value => post.id%>

  <%= ff.fields_for :questions do |builder| %>
    <%= render "question_fields", :f => builder %>
  <% end %>
  <%# = link_to_add_fields "Add Question", ff, :questions %>
</fieldset>