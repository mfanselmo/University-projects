<%= form_with(model: postulation, local: true) do |form| %>
  <% if postulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postulation.errors.count, "error") %> prohibited this postulation from being saved:</h2>

      <ul>
      <% postulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :postulation_user_id %>
  </div>

  <div class="field">
    <%= form.label :forum_id %>
    <%= form.number_field :forum_id, id: :postulation_forum_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
